<div class="row mb-4">
    <div class="offset-1 col-3">
        <a href="{{ route('cars.index', app()->getLocale()) }}"
           class="btn btn-primary">{{ __('dashboard.general.back') }}</a>
    </div>
</div>
